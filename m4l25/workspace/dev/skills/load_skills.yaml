# Dev workspace 专属 Skills 清单
# 数字员工的能力完全由 Workspace 决定（v3 架构原则）
#
# 加载方式：SkillLoaderTool(skills_dir=workspace/dev/skills)

skills:
  - name: sop_dev
    type: reference   # 参考型：直接注入上下文，不启动沙盒
    enabled: true

  - name: memory-save
    type: task        # 任务型：在沙盒中运行脚本写文件
    enabled: true
